import {StyleSheet} from 'react-native';
import {width} from '../../../../helper/ResponsiveUtils';

export const styles = StyleSheet.create({
  Container404: {
    display: 'flex',
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    height: width,
  },
  img: {
    width: width,
  },
});
